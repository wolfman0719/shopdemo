version: '3.7'
#
services:
services:
  iris:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: shopdemo
    restart: always
    
    ports: 
      - 52778:52773

    volumes:
      - ./:/irisdev/app    

    environment:
    - TZ=JST-9